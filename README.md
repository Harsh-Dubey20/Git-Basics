Merge vs Rebase

Merge

Combines two branches.
Creates a merge commit.
Preserves the complete history.
Safer for shared branches.

Rebase

Replays your branch's commits on top of another branch.
Creates a cleaner, linear history.
Avoid rebasing branches that others are already using because it rewrites commit history.
